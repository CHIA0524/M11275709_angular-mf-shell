name: Deploy Shell to GitHub Pages

on:
  push:
    branches:
      - main
      - master
    paths:
      - src/**
      - public/**
      - scripts/**
      - angular.json
      - federation.config.js
      - package.json
      - package-lock.json
      - tsconfig.app.json
      - tsconfig.federation.json
      - tsconfig.json
      - tsconfig.spec.json
      - .github/workflows/deploy-github-pages.yml
  workflow_dispatch:
    inputs:
      comparison_profile:
        description: Compare cold or warm CI baseline
        required: true
        default: warm
        type: choice
        options:
          - cold
          - warm

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NG_PERSISTENT_BUILD_CACHE: 1
      NODE_OPTIONS: --max-old-space-size=6144

    steps:
      - name: Resolve comparison profile
        id: comparison-profile
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            PROFILE="${{ github.event.inputs.comparison_profile }}"
          else
            PROFILE="warm"
          fi

          echo "profile=$PROFILE" >> "$GITHUB_OUTPUT"
          echo "Using comparison profile: $PROFILE"

      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js (warm cache)
        if: steps.comparison-profile.outputs.profile == 'warm'
        uses: actions/setup-node@v6
        with:
          node-version: 20.19.0
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Setup Node.js (cold baseline)
        if: steps.comparison-profile.outputs.profile == 'cold'
        uses: actions/setup-node@v6
        with:
          node-version: 20.19.0

      - name: Restore node_modules cache
        if: steps.comparison-profile.outputs.profile == 'warm'
        id: cache-node-modules
        uses: actions/cache@v5
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-shell-node-20.19.0-${{ hashFiles('package-lock.json') }}

      - name: Restore Angular cache
        if: steps.comparison-profile.outputs.profile == 'warm'
        uses: actions/cache@v5
        with:
          path: .angular/cache
          key: ${{ runner.os }}-angular-shell-${{ hashFiles('package-lock.json', 'angular.json', 'federation.config.js', 'tsconfig*.json') }}
          restore-keys: |
            ${{ runner.os }}-angular-shell-

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Install dependencies
        shell: bash
        run: |
          echo "Install mode: ${{ steps.comparison-profile.outputs.profile }}"
          if [ "${{ steps.comparison-profile.outputs.profile }}" = "warm" ] && [ "${{ steps.cache-node-modules.outputs.cache-hit }}" = "true" ]; then
            echo "node_modules cache hit — skipping npm ci"
          else
            npm ci --no-audit --no-fund
          fi

      - name: Restore Native Federation cache
        if: steps.comparison-profile.outputs.profile == 'warm'
        uses: actions/cache@v5
        with:
          path: node_modules/.cache/native-federation
          key: ${{ runner.os }}-native-federation-shell-${{ hashFiles('package-lock.json', 'angular.json', 'federation.config.js', 'tsconfig*.json') }}
          restore-keys: |
            ${{ runner.os }}-native-federation-shell-

      - name: Apply production federation manifest
        shell: pwsh
        run: ./scripts/use-production-manifest.ps1

      - name: Build Angular app
        run: npm run build -- --configuration production --base-href "/M11275709_angular-mf-shell/"

      - name: Bundle size report
        shell: bash
        run: |
          DIST="dist/shell/browser"
          echo "=== Bundle Size Report ==="
          find "$DIST" -maxdepth 1 -name "*.js" | sort | while read -r f; do
            printf "%8.1f kB  %s\n" "$(echo "$(wc -c < "$f") / 1024" | bc -l)" "$(basename $f)"
          done
          TOTAL=$(find "$DIST" -maxdepth 1 -name "*.js" -exec wc -c {} + | tail -1 | awk '{printf "%.1f", $1/1024}')
          echo "-------------------------------"
          echo "Total JS: ${TOTAL} kB"

      - name: Prepare Pages artifact
        shell: bash
        run: |
          cp dist/shell/browser/index.html dist/shell/browser/404.html
          touch dist/shell/browser/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist/shell/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
